# Camera RTSP credentials — copy this file to ".env" and fill in your values.
# The real ".env" is gitignored, so your password never reaches GitHub.
#
#   cp .env.example .env   # then edit .env with your camera's RTSP login

RTSP_USER=your_camera_rtsp_username
RTSP_PASS=your_camera_rtsp_password
